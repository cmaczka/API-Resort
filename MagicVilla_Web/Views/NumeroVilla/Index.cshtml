@model IEnumerable<MagicVilla_Web.Models.NumeroVillaDto>
@{
    ViewData["Title"] = "IndexNumeroVilla";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container p-4 border">
    <div class="row pb-2">
        <div class="col">
            <h1 class="text-primary">Lista de Numero de Villas</h1>
        </div>
        <div class="col text-end pt-1">
            <a asp-controller="NumeroVilla" asp-action="CreateNuevaVilla" class="btn btn-outline-primary"><i class="bi bi-plus-square-fill"></i>Agregar Numero de Villa</a>
        </div>
    </div>

    <table class="table">
        <thead>
        <th>Nombre de la Villa</th>
        <th>Numero de la Villa</th>
        <th></th>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Villa.Nombre</td>
                    <td>@item.VillaNo</td>
                    <td>
                        <a asp-controller="NumeroVilla" asp-action="ActualizarNumeroVilla" asp-route-villaNo="@item.VillaNo" class="btn btn-outline-primary"><i class="bi bi-pencil-square"></i>Editar</a>
                        <a asp-controller="NumeroVilla" asp-action="RemoverNumeroVilla" asp-route-villaNo="@item.VillaNo" class="btn btn-outline-danger"><i class="bi bi-trash-fill"></i>Eliminar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

